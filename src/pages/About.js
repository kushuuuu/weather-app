import React from "react";

const About = () => (
  <div style={{
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    minHeight: "80vh",
    textAlign: "center"
  }}>
    <h1>About</h1>
    <p>This app shows weather information for various places.</p>
  </div>
);

export default About;
